<div class="contents row">
  <p><%= @name %>さんの投稿一覧</p>
  <% @myarticle.each do |article| %>
    <div class="content_post">
        <%= image_tag article.image.variant(resize:'100x100')%>
      <p>詳細</p>
    </div>
  <% end %>